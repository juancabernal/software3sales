info:
  name: Consultar reserva
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/commercial/api/v1/tables/reservations/search?guestDocumentNumber=12345
  params:
    - name: guestDocumentNumber
      value: "12345"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
