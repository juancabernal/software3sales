info:
  name: Consultar sesion de mesa
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/commercial/api/v1/tables?session=active
  params:
    - name: session
      value: active
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
