meta {
  name: Patch
  type: http
  seq: 6
}

patch {
  url: http://localhost:8080/inventory/product/d8cf2e92-af13-4792-b66c-522d3f38d24c
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Tomate",
    "salePrice": 5300.000,
    "stock": 10.000
  }
}

tests {
  test("Patch exitoso", () => {
    expect(res.status).to.equal(200);
  });
  
  test("Precio actualizado", () => {
    expect(res.body.salePrice).to.equal(5300);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
