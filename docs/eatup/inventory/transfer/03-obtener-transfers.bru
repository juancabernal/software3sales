meta {
  name: 03-obtener-transfers
  type: http
  seq: 3
}

get {
  url: http://localhost:8080/inventory/api/v1/transfers
  auth: inherit
}

tests {
  test("Lista obtenida correctamente", () => {
    expect(res.status).to.equal(200);
  });
  
  test("Es un array", () => {
    expect(res.body).to.be.an("array");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
